turbo-frame id="documents_table" data-turbo-frame="documents_view"
  div.table-container
    div.table-table  
      div.table-row-head
        div.table-cell-head.long-field = "nombre"
        div.table-cell-head.medium-field = "module"
        div.table-cell-head.long-field = "descripción"
        div.table-cell-head.medium-field = "engine"
        div.table-cell-head.medium-field = "path"

      - @objects.each.with_index do |object, index|
        //turbo-frame id="people_#{object.id}"
        a.table-row-body href="/document/#{object.id}" data-turbo-frame="main_frame"  style="cursor:pointer;"
          div.table-cell-body.medium-field  = object.name
          div.table-cell-body.medium-field  = object.pulpo_module.name
          div.table-cell-body.medium-field  = object.description
          div.table-cell-body.medium-field  = object.engine
          div.table-cell-body.medium-field  = object.path
            
                  
              

        
        