turbo-frame id="tasks_table"
  div.table-container
    div.table-table  
      div.table-row-head
        div.table-cell-head.xlong-field = "nombre"

      - @objects.each.with_index do |object, index|
        //turbo-frame id="people_#{object.id}"
        a.table-row-body href="/matrix/task/#{object.id}" data-turbo-frame="main_frame"
          div.table-cell-body  = object.name
         
            
                  
              

        
        